# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# DigitalOcean Container Registry Token
# 從 DigitalOcean 控制台 > API > Tokens 取得
KAMAL_REGISTRY_PASSWORD=$(bin/cred get -e production kamal.KAMAL_REGISTRY_PASSWORD)

# Rails Master Key - 從 config/master.key 讀取
RAILS_MASTER_KEY=$(cat config/master.key)

# PostgreSQL 資料庫密碼 - 強烈建議使用複雜密碼
# 可從 ENV 或密碼管理器取得
POSTGRES_USER=$(bin/cred get -e production database.username)
POSTGRES_PASSWORD=$(bin/cred get -e production database.password)

# Redis URL - 用於 Solid Queue
# 格式: redis://:password@host:port/db
# 本地 Redis 使用: redis://localhost:6379/0
REDIS_URL=$REDIS_URL

# LINE Channel Credentials - 從 LINE Developer Console 取得
# LINE_CHANNEL_ID=$LINE_CHANNEL_ID
# LINE_CHANNEL_SECRET=$LINE_CHANNEL_SECRET

# 使用密碼管理器的範例 (1password)
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item \
#   KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY DATABASE_PASSWORD REDIS_URL)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
# DATABASE_PASSWORD=$(kamal secrets extract DATABASE_PASSWORD ${SECRETS})
# REDIS_URL=$(kamal secrets extract REDIS_URL ${SECRETS})
