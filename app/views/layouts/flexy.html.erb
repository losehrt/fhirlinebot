<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "FHIR LINE Bot - Healthcare Platform" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Google Fonts - DM Sans -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "rails_block", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "flexy", "data-turbo-track": "reload" if File.exist?(Rails.root.join('app/assets/stylesheets/flexy.css')) %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="flexy-layout">
    <div class="flexy-container">
      <!-- Sidebar -->
      <aside class="flexy-sidebar" data-controller="sidebar" data-sidebar-expanded-value="true">
        <!-- Sidebar Header -->
        <div class="sidebar-header">
          <div class="sidebar-logo">
            <span class="logo-icon">
              <%= healthicon('body', 'heart_organ', style: 'filled', class: 'text-line-green-500', width: '32', height: '32') %>
            </span>
            <div class="logo-text" data-sidebar-target="logoText">
              <h1>FHIR LINE Bot</h1>
              <span class="logo-subtitle">Healthcare Platform</span>
            </div>
          </div>
          <button class="sidebar-toggle" data-action="click->sidebar#toggle">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M3 12h18M3 6h18M3 18h18" />
            </svg>
          </button>
        </div>

        <!-- User Profile Section -->
        <% if logged_in? %>
          <div class="sidebar-profile" data-sidebar-target="profile">
            <% if current_user&.line_account&.picture_url %>
              <img src="<%= current_user.line_account.picture_url %>" alt="<%= current_user.name %>" class="profile-avatar">
            <% else %>
              <div class="profile-avatar profile-avatar-placeholder">
                <%= current_user.name.chars.first.upcase %>
              </div>
            <% end %>
            <div class="profile-info" data-sidebar-target="profileInfo">
              <span class="profile-name"><%= current_user.name %></span>
              <span class="profile-role">使用者</span>
            </div>
          </div>
        <% end %>

        <!-- Navigation Menu -->
        <nav class="sidebar-nav">
          <div class="nav-section">
            <%= link_to setup_path, class: "nav-item #{'active' if current_page?(setup_path)}" do %>
              <span class="nav-icon">
                <%= healthicon('devices', 'mobile', style: 'outline', size: '24px', class: 'inline-block align-middle') %>
              </span>
              <span class="nav-text" data-sidebar-target="navText">LINE 設定</span>
            <% end %>
          </div>
        </nav>
      </aside>

      <!-- Main Content Area -->
      <div class="flexy-main">
        <!-- Top Bar -->
        <header class="flexy-topbar">
          <div class="topbar-left">
            <!-- Mobile Menu Toggle -->
            <button class="mobile-menu-toggle" data-action="click->sidebar#toggle">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M3 12h18M3 6h18M3 18h18" />
              </svg>
            </button>

            <!-- Search Bar -->
            <div class="topbar-search" data-controller="search">
              <svg class="search-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="11" cy="11" r="8" />
                <path d="m21 21-4.35-4.35" />
              </svg>
              <input
                type="text"
                placeholder="搜尋..."
                class="search-input"
                data-search-target="input"
                data-action="input->search#query"
              >
            </div>
          </div>

          <div class="topbar-right">
            <!-- Notifications -->
            <button class="topbar-btn" title="通知">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9" />
                <path d="M13.73 21a2 2 0 0 1-3.46 0" />
              </svg>
              <span class="notification-badge">3</span>
            </button>

            <!-- Messages -->
            <button class="topbar-btn" title="訊息">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" />
              </svg>
            </button>

            <!-- User Menu -->
            <div class="topbar-user" data-controller="dropdown">
              <button class="user-menu-btn" data-dropdown-target="button" data-action="click->dropdown#toggle">
                <% if logged_in? %>
                  <% if current_user&.line_account&.picture_url %>
                    <img src="<%= current_user.line_account.picture_url %>" alt="<%= current_user.name %>" class="user-avatar">
                  <% else %>
                    <div class="user-avatar user-avatar-placeholder">
                      <%= current_user.name.chars.first.upcase %>
                    </div>
                  <% end %>
                  <span class="user-name"><%= current_user.name %></span>
                <% else %>
                  <span class="user-name">訪客</span>
                <% end %>
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <polyline points="6 9 12 15 18 9" />
                </svg>
              </button>

              <!-- Dropdown Menu -->
              <div class="user-dropdown hidden" data-dropdown-target="menu">
                <% if logged_in? %>
                  <%= link_to profile_path, class: "dropdown-item" do %>
                    <span>👤 個人檔案</span>
                  <% end %>
                  <div class="dropdown-divider"></div>
                  <%= button_to logout_path, method: :post, class: "dropdown-item" do %>
                    <span>🚪 登出</span>
                  <% end %>
                <% else %>
                  <div class="dropdown-item">
                    <span>請先登入</span>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </header>

        <!-- Page Content -->
        <main class="flexy-content">
          <!-- Flash Messages -->
          <% if flash[:notice] %>
            <div class="flexy-alert alert-success" data-controller="alert" data-alert-auto-dismiss-value="5000">
              <div class="alert-content">
                <span class="alert-icon">
                  <%= healthicon('symbols', 'yes', style: 'filled', size: '20px', class: 'inline-block') %>
                </span>
                <p><%= flash[:notice] %></p>
              </div>
              <button data-action="click->alert#dismiss" class="alert-close">
                <%= healthicon('symbols', 'no', style: 'outline', size: '16px', class: 'inline-block') %>
              </button>
            </div>
          <% end %>

          <% if flash[:alert] %>
            <div class="flexy-alert alert-error" data-controller="alert" data-alert-auto-dismiss-value="5000">
              <div class="alert-content">
                <span class="alert-icon">
                  <%= healthicon('symbols', 'alert_triangle', style: 'filled', size: '20px', class: 'inline-block') %>
                </span>
                <p><%= flash[:alert] %></p>
              </div>
              <button data-action="click->alert#dismiss" class="alert-close">
                <%= healthicon('symbols', 'no', style: 'outline', size: '16px', class: 'inline-block') %>
              </button>
            </div>
          <% end %>

          <!-- Dynamic Page Content -->
          <%= yield %>
        </main>

        <!-- Footer -->
        <footer class="flexy-footer">
          <div class="footer-content">
            <p>&copy; 2025 FHIR LINE Bot. All rights reserved.</p>
            <p class="footer-subtitle">Healthcare Communication Platform | Powered by Rails 8 + LINE Login</p>
          </div>
        </footer>
      </div>
    </div>

    <!-- Initialize LINE Login (for non-logged-in users) -->
    <% unless logged_in? %>
      <script>
        document.addEventListener('DOMContentLoaded', function() {
          // LINE login initialization code here
        });
      </script>
    <% end %>
  </body>
</html>