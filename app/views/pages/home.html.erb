<!-- Landing Page with Modern Design -->
<div>
  <% if !logged_in? %>
    <!-- Announcement Marquee -->
    <div class="w-full">
      <%= render 'shared/marquee', announcements: [
        { text: 'FHIR LINE Bot 平台正式上線' },
        { text: '已整合超過 10 家醫療院所' },
        { text: '符合 FHIR R4 國際標準' },
        { text: '支援即時醫療資料查詢' },
        { text: '提供安全可靠的醫療資訊服務' }
      ], speed: 20, pause_on_hover: true %>
    </div>

    <!-- Hero Section -->
    <section class="relative overflow-hidden bg-orange-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 relative z-10">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <!-- Left Content -->
          <div class="space-y-8">
            <div class="inline-flex items-center px-4 py-2 bg-orange-100 rounded-full">
              <span class="w-2 h-2 bg-orange-500 rounded-full animate-pulse mr-2"></span>
              <span class="text-sm font-medium text-orange-700">FHIR 標準醫療通訊平台</span>
            </div>

            <h1 class="text-5xl lg:text-6xl font-bold text-gray-900 leading-tight">
              智慧醫療
              <span class="text-orange-500">
                即時通訊
              </span>
            </h1>

            <p class="text-xl text-gray-600 leading-relaxed">
              FHIR LINE Bot 整合國際醫療資料標準與 LINE 即時通訊，
              為醫療機構與病患建立安全、便捷的溝通橋樑。
            </p>

            <div class="flex flex-col sm:flex-row gap-4">
              <div id="line-login-hero" class="inline-block"></div>
              <a href="#features" class="inline-flex items-center justify-center px-6 py-3 border-2 border-gray-300 text-gray-700 font-semibold rounded-lg hover:bg-gray-50 transition-colors">
                了解更多
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </a>
            </div>
          </div>

          <!-- Right Visual -->
          <div class="relative">
            <div class="relative bg-white border-2 border-orange-200 rounded-2xl p-8">
              <div class="space-y-6">
                <!-- Mock Chat Interface -->
                <div class="flex items-center space-x-3 pb-4 border-b border-gray-200">
                  <div class="w-10 h-10 bg-orange-500 rounded-full">
                    <%= image_tag "logo-sm.drawio.svg", alt: "FHIRLINEBot", class: "max-w-[40px] max-h-[40px] w-auto h-auto object-contain" %>
                  </div>
                  <div>
                    <p class="font-semibold text-gray-900">FHIRLineBot 火線人</p>
                    <p class="text-xs text-green-600">線上</p>
                  </div>
                </div>

                <!-- Mock Messages -->
                <div class="space-y-4">
                  <div class="flex justify-start">
                    <div class="max-w-[80%] bg-gray-100 rounded-2xl rounded-tl-none p-3">
                      <p class="text-sm text-gray-700">您好！我是 FHIR Bot 醫療助理</p>
                    </div>
                  </div>
                  <div class="flex justify-end">
                    <div class="max-w-[80%] bg-green-500 text-white rounded-2xl rounded-tr-none p-3">
                      <p class="text-sm">請幫我查詢最近的檢驗報告</p>
                    </div>
                  </div>
                  <div class="flex justify-start">
                    <div class="max-w-[80%] bg-gray-100 rounded-2xl rounded-tl-none p-3">
                      <p class="text-sm text-gray-700">正在為您查詢...</p>
                      <div class="flex space-x-1 mt-2">
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce"></div>
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.1s"></div>
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.2s"></div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

    </section>

    <!-- Features Section -->
    <section id="features" class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">核心功能</h2>
          <p class="text-xl text-gray-600">整合醫療標準與即時通訊的創新解決方案</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- Feature 1 -->
          <div class="group relative h-full">
            <div class="absolute inset-0 bg-orange-100 rounded-2xl transform rotate-1 group-hover:rotate-2 transition-transform"></div>
            <div class="relative bg-white border border-gray-200 rounded-2xl p-8 hover:shadow-lg transition-shadow z-10 h-full flex flex-col">
              <div class="w-14 h-14 bg-orange-500 rounded-xl flex items-center justify-center mb-6">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                </svg>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-3">FHIR 標準整合</h3>
              <p class="text-gray-600 flex-grow">
                完全符合 HL7 FHIR 國際醫療資料交換標準，確保資料互通性與準確性
              </p>
            </div>
          </div>

          <!-- Feature 2 -->
          <div class="group relative h-full">
            <div class="absolute inset-0 bg-green-100 rounded-2xl transform -rotate-1 group-hover:-rotate-2 transition-transform"></div>
            <div class="relative bg-white border border-gray-200 rounded-2xl p-8 hover:shadow-lg transition-shadow z-10 h-full flex flex-col">
              <div class="w-14 h-14 bg-green-500 rounded-xl flex items-center justify-center mb-6">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"/>
                </svg>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-3">LINE 即時通訊</h3>
              <p class="text-gray-600 flex-grow">
                透過 LINE 平台提供親切便利的醫療諮詢與通知服務
              </p>
            </div>
          </div>

          <!-- Feature 3 -->
          <div class="group relative h-full">
            <div class="absolute inset-0 bg-blue-100 rounded-2xl transform rotate-1 group-hover:rotate-2 transition-transform"></div>
            <div class="relative bg-white border border-gray-200 rounded-2xl p-8 hover:shadow-lg transition-shadow z-10 h-full flex flex-col">
              <div class="w-14 h-14 bg-blue-500 rounded-xl flex items-center justify-center mb-6">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                </svg>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-3">資安防護</h3>
              <p class="text-gray-600 flex-grow">
                採用端對端加密與 OAuth 2.0 認證，保護醫療隱私資料安全
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- How It Works Section -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">使用流程</h2>
          <p class="text-xl text-gray-600">簡單三步驟，立即開始使用</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- Step 1 -->
          <div class="text-center">
            <div class="relative inline-block mb-6">
              <div class="w-20 h-20 bg-orange-500 rounded-full flex items-center justify-center text-white text-2xl font-bold">
                1
              </div>
              <div class="absolute -right-full top-1/2 hidden md:block w-full">
                <svg class="w-full" height="2" fill="none">
                  <path d="M0 1L200 1" stroke="#FB923C" stroke-width="2" stroke-dasharray="5 5" opacity="0.3"/>
                </svg>
              </div>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3">LINE 登入</h3>
            <p class="text-gray-600">使用您的 LINE 帳號安全登入平台</p>
          </div>

          <!-- Step 2 -->
          <div class="text-center">
            <div class="relative inline-block mb-6">
              <div class="w-20 h-20 bg-green-500 rounded-full flex items-center justify-center text-white text-2xl font-bold">
                2
              </div>
              <div class="absolute -right-full top-1/2 hidden md:block w-full">
                <svg class="w-full" height="2" fill="none">
                  <path d="M0 1L200 1" stroke="#10B981" stroke-width="2" stroke-dasharray="5 5" opacity="0.3"/>
                </svg>
              </div>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3">連結醫療機構</h3>
            <p class="text-gray-600">與您的醫療服務提供者建立連結</p>
          </div>

          <!-- Step 3 -->
          <div class="text-center">
            <div class="inline-block mb-6">
              <div class="w-20 h-20 bg-blue-500 rounded-full flex items-center justify-center text-white text-2xl font-bold">
                3
              </div>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3">開始使用</h3>
            <p class="text-gray-600">接收醫療通知與查詢健康資訊</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 bg-green-500">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-4xl font-bold text-white mb-6">準備好開始了嗎？</h2>
        <p class="text-xl text-white/90 mb-8">
          立即使用 LINE 登入，體驗智慧醫療通訊服務
        </p>
        <div id="line-login-cta" class="inline-block"></div>
      </div>
    </section>

    <!-- Initialize LINE Login Buttons -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Helper function to create LINE login button
        function createLineLoginButton(containerId, buttonClass = '') {
          const container = document.getElementById(containerId);
          if (container) {
            const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content || '';
            fetch('<%= auth_request_login_path(format: :json) %>', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json',
                'X-CSRF-Token': csrfToken
              },
              credentials: 'same-origin'
            })
            .then(response => {
              if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
              }
              return response.json();
            })
            .then(data => {
              const button = document.createElement('a');
              button.href = data.authorization_url;
              button.className = buttonClass || 'inline-flex items-center space-x-2 px-6 py-3 bg-[#00b900] hover:bg-[#009900] text-white font-semibold rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl hover:scale-105';
              button.innerHTML = `
                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M19 3H5C3.9 3 3 3.9 3 5V19C3 20.1 3.9 21 5 21H19C20.1 21 21 20.1 21 19V5C21 3.9 20.1 3 19 3M19 19H5V5H19V19M17 8.5C17 8.22 16.78 8 16.5 8H14.3C13.9 8 13.5 8.2 13.3 8.5L12 10.9L10.7 8.5C10.5 8.2 10.1 8 9.7 8H7.5C7.22 8 7 8.22 7 8.5V15.5C7 15.78 7.22 16 7.5 16H8.5C8.78 16 9 15.78 9 15.5V11.58L10.3 13.87C10.5 14.19 10.9 14.4 11.3 14.4H12.7C13.1 14.4 13.5 14.19 13.7 13.87L15 11.58V15.5C15 15.78 15.22 16 15.5 16H16.5C16.78 16 17 15.78 17 15.5V8.5Z"/>
                </svg>
                <span>使用 LINE 登入</span>
              `;
              container.appendChild(button);
            })
            .catch(error => {
              console.error('Failed to load LINE login:', error);
            });
          }
        }

        // Create buttons
        createLineLoginButton('line-login-hero');
        createLineLoginButton('line-login-cta', 'inline-flex items-center space-x-2 px-8 py-4 bg-white text-green-600 font-bold text-lg rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl hover:scale-105');
      });
    </script>

  <% else %>
    <!-- Logged-in User Dashboard -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Welcome Header -->
      <div class="bg-gradient-to-r from-orange-500 to-green-500 rounded-2xl p-8 mb-8 text-white">
        <h1 class="text-3xl font-bold mb-2">
          歡迎回來，<%= current_user.display_name %>！
        </h1>
        <p class="text-white/90">
          FHIR LINE Bot 醫療通訊平台
        </p>
      </div>

      <!-- Quick Stats -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <!-- LINE Connection Status -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            </div>
            <% if current_user.has_line_account? %>
              <span class="px-3 py-1 bg-green-100 text-green-700 text-sm font-medium rounded-full">已連結</span>
            <% else %>
              <span class="px-3 py-1 bg-gray-100 text-gray-700 text-sm font-medium rounded-full">未連結</span>
            <% end %>
          </div>
          <h3 class="text-gray-600 text-sm mb-1">LINE 帳號</h3>
          <p class="text-2xl font-bold text-gray-900">
            <%= current_user.has_line_account? ? current_user.line_account.display_name : "尚未連結" %>
          </p>
        </div>

        <!-- User Profile -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
              <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
            </div>
            <span class="text-sm text-gray-500">ID: <%= current_user.id.to_s.first(8) %>...</span>
          </div>
          <h3 class="text-gray-600 text-sm mb-1">使用者名稱</h3>
          <p class="text-2xl font-bold text-gray-900">
            <%= current_user.name %>
          </p>
        </div>

        <!-- Member Since -->
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
          <div class="flex items-center justify-between mb-4">
            <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
              <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            </div>
            <span class="text-sm text-gray-500"><%= (Date.current - current_user.created_at.to_date).to_i %> 天</span>
          </div>
          <h3 class="text-gray-600 text-sm mb-1">加入日期</h3>
          <p class="text-2xl font-bold text-gray-900">
            <%= current_user.created_at.strftime("%Y/%m/%d") %>
          </p>
        </div>
      </div>

      <!-- LINE Account Details -->
      <% if current_user.has_line_account? %>
        <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 mb-8">
          <h2 class="text-2xl font-bold text-gray-900 mb-6">LINE 帳號資訊</h2>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <!-- Profile Info -->
            <div class="flex items-start space-x-4">
              <% if current_user.line_account.picture_url %>
                <img
                  src="<%= current_user.line_account.picture_url %>"
                  alt="<%= current_user.line_account.display_name %>"
                  class="w-20 h-20 rounded-xl border-4 border-green-100"
                >
              <% else %>
                <div class="w-20 h-20 rounded-xl bg-gradient-to-br from-green-400 to-green-600 flex items-center justify-center text-white text-2xl font-bold">
                  <%= current_user.name.chars.first.upcase %>
                </div>
              <% end %>

              <div class="flex-1">
                <h3 class="font-bold text-gray-900 text-lg mb-1">
                  <%= current_user.line_account.display_name %>
                </h3>
                <p class="text-sm text-gray-500 font-mono mb-3">
                  ID: <%= current_user.line_account.line_user_id %>
                </p>
                <p class="text-sm text-gray-600">
                  上次同步：<%= current_user.line_account.updated_at.strftime("%Y/%m/%d %H:%M") %>
                </p>
              </div>
            </div>

            <!-- Token Status -->
            <div class="space-y-4">
              <div class="p-4 bg-gray-50 rounded-lg">
                <h4 class="text-sm font-medium text-gray-700 mb-2">認證狀態</h4>
                <% if current_user.line_account.access_token_expired? %>
                  <div class="flex items-center text-red-600">
                    <svg class="w-5 h-5 mr-2 inline-block" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                    </svg>
                    <span class="font-medium">Token 已過期，需要重新登入</span>
                  </div>
                <% else %>
                  <div class="flex items-center text-green-600">
                    <svg class="w-5 h-5 mr-2 inline-block" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <span class="font-medium">有效（還有 <%= ((current_user.line_account.expires_at - Time.current).to_i / 86400) %> 天）</span>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% else %>
        <!-- No LINE Account Connected -->
        <div class="bg-orange-50 border border-orange-200 rounded-xl p-8 mb-8 text-center">
          <div class="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-orange-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-2">尚未連結 LINE 帳號</h3>
          <p class="text-gray-600 mb-4">連結您的 LINE 帳號以使用完整功能</p>
          <div id="line-connect-dashboard" class="inline-block"></div>
        </div>
      <% end %>

      <!-- Quick Actions -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <%= link_to profile_path, class: "group bg-white rounded-xl shadow-sm border border-gray-200 p-6 hover:shadow-lg hover:border-orange-300 transition-all duration-200" do %>
          <div class="flex items-center space-x-4">
            <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center group-hover:bg-orange-200 transition-colors">
              <svg class="w-6 h-6 text-orange-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
              </svg>
            </div>
            <div>
              <h3 class="font-bold text-gray-900">個人資料</h3>
              <p class="text-sm text-gray-600">查看與編輯您的個人資訊</p>
            </div>
            <svg class="w-5 h-5 text-gray-400 group-hover:text-orange-600 transition-colors ml-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </div>
        <% end %>

        <%= link_to setup_path, class: "group bg-white rounded-xl shadow-sm border border-gray-200 p-6 hover:shadow-lg hover:border-green-300 transition-all duration-200" do %>
          <div class="flex items-center space-x-4">
            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center group-hover:bg-green-200 transition-colors">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            </div>
            <div>
              <h3 class="font-bold text-gray-900">LINE 設定</h3>
              <p class="text-sm text-gray-600">管理 LINE Bot 連線設定</p>
            </div>
            <svg class="w-5 h-5 text-gray-400 group-hover:text-green-600 transition-colors ml-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Initialize LINE Connect Button for Dashboard -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const container = document.getElementById('line-connect-dashboard');
        if (container) {
          const csrfToken = document.querySelector('meta[name="csrf-token"]')?.content || '';
          fetch('<%= request_line_login_path(format: :json) %>', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
              'X-CSRF-Token': csrfToken
            },
            credentials: 'same-origin'
          })
          .then(response => response.json())
          .then(data => {
            const button = document.createElement('a');
            button.href = data.authorization_url;
            button.className = 'inline-flex items-center space-x-2 px-6 py-3 bg-[#00b900] hover:bg-[#009900] text-white font-semibold rounded-lg transition-colors duration-200';
            button.innerHTML = `
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M19 3H5C3.9 3 3 3.9 3 5V19C3 20.1 3.9 21 5 21H19C20.1 21 21 20.1 21 19V5C21 3.9 20.1 3 19 3M19 19H5V5H19V19M17 8.5C17 8.22 16.78 8 16.5 8H14.3C13.9 8 13.5 8.2 13.3 8.5L12 10.9L10.7 8.5C10.5 8.2 10.1 8 9.7 8H7.5C7.22 8 7 8.22 7 8.5V15.5C7 15.78 7.22 16 7.5 16H8.5C8.78 16 9 15.78 9 15.5V11.58L10.3 13.87C10.5 14.19 10.9 14.4 11.3 14.4H12.7C13.1 14.4 13.5 14.19 13.7 13.87L15 11.58V15.5C15 15.78 15.22 16 15.5 16H16.5C16.78 16 17 15.78 17 15.5V8.5Z"/>
              </svg>
              <span>連結 LINE 帳號</span>
            `;
            container.appendChild(button);
          })
          .catch(error => console.error('Failed to load LINE connect:', error));
        }
      });
    </script>
  <% end %>
</div>